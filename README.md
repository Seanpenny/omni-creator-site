# Omni Creator Site

Tiny public site for platform app verification.

Use these routes after deploying to Vercel:

- `/`
- `/terms`
- `/privacy`

If TikTok gives a verification meta tag, replace the placeholder meta tag in `index.html`.
If TikTok gives an HTML verification file, place it in this folder before deployment.
